<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    tools:background="#FFFFFF">

    <ImageView
        android:id="@+id/custom_text_view_icon"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:padding="2dp"
        android:scaleType="centerInside"
        android:tint="@color/colorAccent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription"
        tools:src="@drawable/ic_clock" />

    <TextView
        android:id="@+id/custom_text_view_heading"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="6dp"
        android:textIsSelectable="false"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/custom_text_view_icon"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/custom_text_view_icon"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="10:30PM" />

    <TextView
        android:id="@+id/custom_text_view_subheading"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textIsSelectable="false"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@id/custom_text_view_heading"
        app:layout_constraintTop_toBottomOf="@id/custom_text_view_heading"
        tools:text="Sunday" />

</androidx.constraintlayout.widget.ConstraintLayout>